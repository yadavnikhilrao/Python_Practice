# Function Defination
def add(n1,n2): # function Parameters n1 and n2
    return n1+n2

# Function Call
add(5,5) # function Arguments

### Parameters
1. We defined the parameters when defining the function.
2. To get the value from the arguments, we utilized the local variables in the function declaration.
3. When we invoked the function, the values of the arguments are assigned to the local variables .
4. They’re sometimes referred to as Formal Parameters.

### Arguments
1. We used the variables in the function to send the value of the calling function to the receiving function.
2. We used the arguments to transfer values from the calling function to the receiving function in function call statements.
3. We always assigned each argument to the parameter in the function declaration at the moment of the call.
4. Actual Parameters is another name for them.
